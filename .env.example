# Copy file này thành .env và điền giá trị thật — KHÔNG commit .env
AI_SERVICE_PORT=8000
BACKEND_PORT=8080
FRONTEND_PORT=3000

# MongoDB Atlas connection string
MONGO_URI=

# Nếu public bằng ngrok/tunnel, cập nhật link tại đây mỗi lần đổi
PUBLIC_APP_URL=
PUBLIC_AI_SERVICE_URL=
